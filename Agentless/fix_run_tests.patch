*** Begin Patch
*** Update File: agentless/agentless/test/run_tests.py
@@
-from swebench.harness.test_spec.test_spec import TestSpec, GenericTestSpec, ScriptTestSpec
-from swebench.harness.utils import get_test_directives
+from swebench.harness.test_spec.test_spec import TestSpec

@@ def run_reproduction_tests(
-    instances = get_dataset_from_preds(
-        dataset_name, split, instance_ids, predictions, run_id
-    )
+    instances = get_dataset_from_preds(
+        dataset_name, split, instance_ids, predictions, run_id, False
+    )

@@ def run_tests(
-    instances = get_dataset_from_preds(
-        dataset_name, split, instance_ids, predictions, run_id
-    )
+    instances = get_dataset_from_preds(
+        dataset_name, split, instance_ids, predictions, run_id, False
+    )
*** End Patch
